// T-Shirt World — Supabase public configuration
// Only use the Project URL and anon/public key here.
// NEVER put a service_role key, password, PIN, or private API secret in this file.
window.TW_CONFIG = {
  SUPABASE_URL: "YOUR_SUPABASE_PROJECT_URL",
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY"
};
